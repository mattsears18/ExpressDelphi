USERS
    id                (handled auto)
    email             (handled auto)
    password          (handled auto)
    studies           --belongsToMany

STUDIES
    name              String
    desc              String
    currentRound      Integer
    open              Boolean
    alternatives      --hasMany
    criteria          --hasMany
    users             --hasMany

ALTERNATIVES
    name              String
    desc              String
    consensusReached  Boolean
    study             --belongsToOne
    weightVotes       --hasMany
    roundCriteria     --hasMany

CRITERIA
    name              String
    desc              String
    weight            Float
    study             --belongsToOne

ALTERNATIVE_CRITERIA
    consensusReached  Boolean

WEIGHTVOTES
    criterion         --belongsToOne
    user              --belongsToOne
    weight            Integer

RATING
    user              --belongsToOne
    study             --belongsToOne
    roundCriteria     Integer
    alternative       --belongsToOne
    criterion         --belongsToOne
    value             Integer (1 to 10)



select all alternatives that have roundCriteria (consensus not reached yet) that
  match the currentStudy's currentRound and all of the criteria haven't already
  been rated by the current user. Randomly display one of the alternatives to
  the user and ask them to complete the ratings.
