USERS
    id                (handled auto)
    email             (handled auto)
    password          (handled auto)
    studies           --belongsToMany

STUDIES
    name              String
    desc              String
    currentRound      Integer
    open              Boolean
    alternatives      --hasMany
    monkeys           --hasMany
    users             --hasMany

ALTERNATIVES
    name              String
    desc              String
    consensusReached  Boolean
    study             --belongsToOne
    weightVotes       --hasMany

MONKEY
    name              String
    desc              String
    weight            Float
    study             --belongsToOne


WEIGHTVOTES
    monkey            --belongsToOne
    user              --belongsToOne
    weight            Integer


PAIRS
    study             --belongsToOne
    alternative       --belontsToOne
    monkeys           --belongsToOne
    consensusReached  Boolean


RATING
    user              --belongsToOne
    pair              --belongsToOne
    study             --belongsToOne
    alternative       --belongsToOne
    monkeys           --belongsToOne
    value             Integer (1 to 10)



select all alternatives that have roundMonkey (consensus not reached yet) that
  match the currentStudy's currentRound and all of the monkey haven't already
  been rated by the current user. Randomly display one of the alternatives to
  the user and ask them to complete the ratings.


ratingpairs
