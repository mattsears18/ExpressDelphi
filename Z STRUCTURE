USERS
    id                (handled auto)
    email             (handled auto)
    password          (handled auto)
    studies           --belongsToMany

STUDIES
    name              String
    desc              String
    currentRound      Integer
    open              Boolean
    alternatives      --hasMany
    criteria           --hasMany
    users             --hasMany

ALTERNATIVES
    name              String
    desc              String
    consensusReached  Boolean
    study             --belongsToOne
    weightVotes       --hasMany

CRITERION
    name              String
    desc              String
    weight            Float
    study             --belongsToOne


WEIGHTVOTES
    criterion            --belongsToOne
    user              --belongsToOne
    weight            Integer


PAIRS
    study             --belongsToOne
    alternative       --belontsToOne
    criteria           --belongsToOne
    consensusReached  Boolean


RATING
    user              --belongsToOne
    pair              --belongsToOne
    study             --belongsToOne
    alternative       --belongsToOne
    criteria           --belongsToOne
    value             Integer (1 to 10)



select all alternatives that have roundCriterion (consensus not reached yet) that
  match the currentStudy's currentRound and all of the criterion haven't already
  been rated by the current user. Randomly display one of the alternatives to
  the user and ask them to complete the ratings.


ratingpairs
